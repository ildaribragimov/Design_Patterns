// Явное указание на режим строгого соответствия современному стандарту
"use strict";
/**
 * Функция "animate"
 * Выполняет анимацию над элементом
 */
function animate(g,d,f,b){
    function n(a){
        switch(b.easingFunc){
            case "linear":return a;
            case "easeInQuad":return Math.pow(a,2);
            case "easeOutQuad":return 1-Math.pow(1-a,2);
            case "easeInOutQuad":return.5>a?Math.pow(2*a,2)/2:(2-Math.pow(2*(1-a),2))/2;
            case "easeInCubic":return Math.pow(a,3);
            case "easeOutCubic":return 1-Math.pow(1-a,3);
            case "easeInOutCubic":return.5>a?Math.pow(2*a,3)/2:(2-Math.pow(2*(1-a),3))/2;
            case "easeInQuint":return Math.pow(a,5);
            case "easeOutQuint":return 1-Math.pow(1-a,5);
            case "easeInOutQuint":return.5>a?Math.pow(2*a,5)/2:(2-Math.pow(2*(1-a),5))/2
        }
        return b.easingFunc(a)
    }
    function k(a){a=(a-p)/b.duration;1<a&&(a=1);var c=n(a),e;for(e in d)d.hasOwnProperty(e)&&(l.style[e]=h[e]+c*m[e]+"px");1>a?requestAnimationFrame(k):f&&f()}f=f||null;b=b||{};b.easingFunc=b.easingFunc||"linear";b.duration=b.duration||1E3;var l=g;g=getComputedStyle(l);var h={},m={},c;for(c in d)d.hasOwnProperty(c)&&(h[c]=parseFloat(g[c]),m[c]=parseFloat(d[c])-h[c]);var p=performance.now();requestAnimationFrame(k)
};