// Явное указание на режим строгого соответствия современному стандарту
"use strict";
/**
 * Функция "animate"
 * Выполняет анимацию над элементом
 */
function animate(h,e,d,g){
    function m(a){
        switch(d.easingFunc){
            case "linear":return a;
            case "easeInQuad":return Math.pow(a,2);
            case "easeOutQuad":return 1-Math.pow(1-a,2);
            case "easeInOutQuad":return.5>a?Math.pow(2*a,2)/2:(2-Math.pow(2*(1-a),2))/2;
            case "easeInCubic":return Math.pow(a,3);
            case "easeOutCubic":return 1-Math.pow(1-a,3);
            case "easeInOutCubic":return.5>a?Math.pow(2*a,3)/2:(2-Math.pow(2*(1-a),3))/2;
            case "easeInQuint":return Math.pow(a,5);
            case "easeOutQuint":return 1-Math.pow(1-a,5);
            case "easeInOutQuint":return.5>a?Math.pow(2*a,5)/2:(2-Math.pow(2*(1-a),5))/2;
        }
        return d.easingFunc(a);
    }
    function k(a){a=(a-n)/d.duration;1<a && (a=1);var b=m(a),f;for(f in e){e.hasOwnProperty(f) && (l.style[f]=c[f].startingValue+b*c[f].deltaValue+c[f].unit);}1>a?requestAnimationFrame(k):g && g();}d=d||{};d.easingFunc=d.easingFunc||"linear";d.duration=d.duration||1E3;g=g||null;var l=h;h=getComputedStyle(l);var c={},b;for(b in e){e.hasOwnProperty(b) && (c[b]={},c[b].startingValue=parseFloat(h[b]),c[b].endingValue=parseFloat(e[b]),c[b].deltaValue=c[b].endingValue-c[b].startingValue,c[b].unit=e[b].toString().substr(c[b].endingValue.toString().length));}var n=performance.now();requestAnimationFrame(k);
};